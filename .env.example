# --- Backend API Connection Settings ---
# Required authentication token for the core estimation API processing instance
VITE_API_KEY="your_api_key_here"

# --- Client Deployment Configuration ---
# The target URL endpoint where the application frontend instance is hosted dynamically
VITE_APP_URL="http://localhost:3000"